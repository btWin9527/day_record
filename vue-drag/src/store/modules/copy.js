import {$} from '@/utitls/utils'

const state = {
  copyData: null, // 复制粘贴剪切
  isCut: false,
}

const mutations = {
  copy(state) {

  }
}

export default {
  namespaced: true,
  state,
  mutations
}