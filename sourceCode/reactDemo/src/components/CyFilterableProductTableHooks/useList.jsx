import React, { useMemo } from 'react';

export default function useList(products) {
  console.log(products);
  const groupProductList = useMemo(()=>{
    return 
  })
  return {
    groupProductList,
  };
}
